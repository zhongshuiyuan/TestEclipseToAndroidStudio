<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#BEC3C7" >

    <com.qingdao.shiqu.arcgis.control.TopBar
        android:id="@+id/topBar"
        android:layout_width="fill_parent"
        android:layout_height="45dp"
        custom:title_text="青岛有线地图应用客户端" />

    <LinearLayout
        android:id="@+id/linearLayout1"
        android:layout_width="50dp"
        android:layout_height="fill_parent"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/topBar"
        android:background="@drawable/role_list_background"
        android:gravity="right"
        android:orientation="vertical" >

        <ListView
            android:id="@+id/RoleListView"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:descendantFocusability="blocksDescendants"
            android:divider="#00000000"
            android:dividerHeight="0dp"
            android:listSelector="#00000000" >
        </ListView>
    </LinearLayout>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@+id/topBar"
        android:layout_toRightOf="@+id/linearLayout1"
        android:orientation="vertical" >

        <ListView
            android:id="@+id/FunctionListView"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:divider="@color/listview_text_read"
            android:dividerHeight="1px" >

        </ListView>
    </LinearLayout>

</RelativeLayout>